---
/**
 * Comparison page (/landing/comparison, English) — "AnvilWiki vs Fandom vs
 * Wiki.js": catches comparison-intent search and hands AI engines citable
 * facts. 中文版在 /zh/landing/comparison。Removed together with the whole
 * landing layer by apply-template (lives under src/pages/landing/).
 */
import LandingLayout from '~/components/landing/LandingLayout.astro';
import ComparisonPage from '~/components/landing/ComparisonPage.astro';
import { landingContent } from '~/config/landing';
import { siteUrl } from '~/config/site';

const t = landingContent.en;
const alternates = [
  { hreflang: 'en', href: `${siteUrl}/landing/comparison` },
  { hreflang: 'zh', href: `${siteUrl}/zh/landing/comparison` },
];
---

<LandingLayout
  locale="en"
  alternates={alternates}
  pageTitle={t.comparisonPage.title}
  pageDescription={t.comparisonPage.subtitle}
  togglePath="/zh/landing/comparison"
>
  <ComparisonPage t={t} />
</LandingLayout>
